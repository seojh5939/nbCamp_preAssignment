//버켓 수정페이지.
import 'package:flutter/material.dart';

class BucketEdit extends StatelessWidget {
  const BucketEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color.fromARGB(255, 230, 229, 229),
      body: Column(
        children: [Text("data")],
      ),
    );
  }
}
